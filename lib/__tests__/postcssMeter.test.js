const assert = require('assert');
const postcssMeter = require('../index.js');

describe('postcssMeter', () => {
  it('has a test', () => {
    assert(false, 'postcssMeter should have a test');
  });
});
